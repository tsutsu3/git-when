# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/
node_modules/
frontend/.astro/
frontend/dist/

# Go workspace file
go.work
go.work.sum

# env file
# Deploys use wrangler auth profiles, so no .env file is needed.
# A .env file could still hold Cloudflare tokens, so ignore every variant.
.env*

# Editor/IDE
# .idea/
.vscode/

/tmp/

# Default output filenames
git-when.html
git-when.csv
git-when.svg

# wrangler
# public/index.html is the demo report. It is built by hand and committed.
/public/*
!/public/_headers
!/public/robots.txt
!/public/index.html
!/public/404.html
.wrangler/
